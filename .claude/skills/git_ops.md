<skill_context>
Version Control Standards
</skill_context>
<rules>
- Output an exhaustive `.gitignore` covering `node_modules`, `.env`, build artifacts, and OS files (`.DS_Store`).
- Commits must use conventional formats (e.g., `feat: added auth`, `fix: token expiration`).
- Include a high-quality `README.md` containing `npm run dev` and `docker-compose up` startup instructions.
</rules>